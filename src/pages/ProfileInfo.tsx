import Header from '../components/common/Header';
import UserInputForm from '../components/ProfileInfo/UserInputForm';

const ProfileInfo = () => {
  return (
    <>
      {/* <Header /> */}
      <UserInputForm />
    </>
  );
};

export default ProfileInfo;

// 1. input 스타일 만들기 ok
// 2. ProfileInfo component에 4가지 인풋 목록 넣기
// 3.ProfileInfo 페이지에 소개글, 인풋 4개, 버튼(2개) 넣기
